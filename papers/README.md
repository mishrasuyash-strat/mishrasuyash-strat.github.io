# Put your PDF papers here

Drop PDF files in this folder (e.g. `my-paper.pdf`), then reference them
from `_data/papers.yml`:

    pdf: "/papers/my-paper.pdf"

That's it — the link will appear on the Papers page.
